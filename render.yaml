services:
  - type: web
    name: rune-split
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false # cole aqui a connection string do Postgres (Neon/Supabase/Render)
